// Schema vazio de propósito: a tabela smoke_test que provou o pipeline de
// migration na ENG-53 foi removida. A modelagem de dados real entra na
// próxima task, começando o histórico de migrations do zero.
export {};
